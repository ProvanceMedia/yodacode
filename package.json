{
  "name": "yodacode",
  "version": "2.7.0",
  "private": true,
  "type": "module",
  "description": "YodaCode root — installer + scripts. The actual agent runtime lives in ./workspace.",
  "engines": {
    "node": ">=20"
  }
}
